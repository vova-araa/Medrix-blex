# Sharzi — voorbeeldconfiguratie. Kopieer naar .env en vul echte waarden in.
# .env komt NOOIT in git (CLAUDE.md §17).

# Supabase (nog niet in gebruik — de app draait op mockdata tot het dev-project bestaat)
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
